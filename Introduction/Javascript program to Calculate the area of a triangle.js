// Javascript program to Calculate the area of a triangle

const base = parseInt(prompt('Enter the base of a triangle'));
const height = parseInt(prompt('Enter the height of a triangle'));

const areaOfTringle = 1/2 * base* height;

console.log(` The area of triangle is ${areaOfTringle}`);


